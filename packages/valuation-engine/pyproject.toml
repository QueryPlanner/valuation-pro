[project]
name = "valuation-engine"
version = "0.1.0"
description = "Pure FCFF Ginzu valuation engine — zero external dependencies"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/valuation_engine"]
